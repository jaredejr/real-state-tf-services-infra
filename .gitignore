# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

plan

# Exclude all .tfvars files, to prevent accidental credential exposure.
# If you use tfvars files, ensure they are not sensitive or use a more specific pattern.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually environment-specific
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include tfplan files to ignore the plan output of command terraform plan
*.tfplan

# Ignore CLI configuration files
.terraformrc
terraform.rc